import { User } from "../../domain/entities/users"


export class UserCollection {

    public static async insert(person:User){
        
        //this.userRef.doc(person._id).set(person.toJson())
    }
}